#!/usr/bin/env sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "❌ Direct commits to 'main' are blocked. Create a feature branch and open a PR."
  exit 1
fi

exit 0
